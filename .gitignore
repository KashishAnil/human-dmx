# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.zip

# The API lives in its own repository (code-verse-dev/dmx-backend) and is
# cloned in here for local development. Keeping it out of this tree also keeps
# its .env files and registry token from ever being staged by a `git add -A`.
backend/