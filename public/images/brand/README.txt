Drop the HUMAN DMX APPAREL badge here as:

    crest.png

Transparent PNG, roughly 3:4 (the badge is taller than it is wide).
1200px tall is plenty — it renders between 56px and 92px on screen.

Until this file exists the site falls back to the wide "HUMAN DMX APPAREL"
text lockup, so nothing appears broken. Once it's here the header, footer,
checkout, merchant login, portal sidebar and the browser tab icon all pick
it up automatically — no code change needed.
